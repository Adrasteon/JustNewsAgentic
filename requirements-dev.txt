# Lightweight dev/test dependencies for CI (Dockerless)
# Keep minimal to ensure fast installs and stable unit runs.

# Linters & static analysis
ruff>=0.5.0
mypy>=1.8.0
bandit>=1.7.6

# Test runner
pytest>=7.4.0

# Minimal runtime deps needed by targeted tests
httpx>=0.27.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0,<3.0.0
requests>=2.31.0
