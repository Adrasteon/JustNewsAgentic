# Synthesizer Agent Dependencies - For conda justnews-production environment
# Note: Docker deployment is obsolete - use conda environment instead

setuptools>=65.0.0
wheel
fastapi
transformers>=4.30.0
torch>=2.0.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
bertopic>=0.17.0  # Advanced topic modeling with UMAP
umap-learn>=0.5.9  # Dimensionality reduction (required by BERTopic)
numpy>=1.24.0,<2.0.0  # Compatible with ML libraries
hdbscan  # Clustering algorithm (required by BERTopic)
uvicorn[standard]>=0.22.0
pydantic>=2.0.0,<3.0.0
protobuf>=6.30.0  # Required for FLAN-T5 models
sentencepiece>=0.2.0  # Tokenization for T5 models  
textstat>=0.7.8  # Text complexity analysis
