[pytest]
pythonpath = .
testpaths = tests
# Ignore legacy and non-test directories to avoid noise in CI/local runs
norecursedirs =
	archive_obsolete_files
	.git
	.venv venv env
	build dist
	model_cache models
	memory_v2_vectordb
	bbc_screenshots crawl4ai_docs
addopts = -ra
