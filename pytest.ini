[pytest]
pythonpath = .
testpaths = tests
addopts = -ra -q
markers =
	unit: quick unit tests
	integration: integration tests that may use services
	e2e: end-to-end tests exercising the full pipeline
	gpu: tests requiring GPU
	slow: long-running tests
asyncio_mode = auto
